# Configuration PostgREST pour Wakaa
db-uri = "postgresql://postgres:Romuald12@localhost:5432/wakaa"
db-schemas = "public"
db-anon-role = "postgres"
db-pool = 10
db-pool-acquisition-timeout = 10

server-host = "localhost"
server-port = 3001

# JWT (optionnel pour le développement)
# jwt-secret = "wakaa-jwt-secret-key"
# jwt-aud = "wakaa"

# CORS
server-cors-allowed-origins = "*"

# Logging
log-level = "info"

# OpenAPI
openapi-mode = "follow-privileges"
openapi-server-proxy-uri = "http://localhost:3001"
